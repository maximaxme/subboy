# 📋 Пошаговая инструкция - пункт 3

## ✅ Шаг 1: Зависимости уже установлены!
Все необходимые пакеты Python уже установлены.

## 🔧 Шаг 2: Создайте базу данных

**Вариант А - через pgAdmin (самый простой):**
1. Откройте **pgAdmin** из меню Пуск
2. Введите пароль от PostgreSQL
3. Правой кнопкой на **"Databases"** → **"Create"** → **"Database"**
4. Имя: `sub_tracker`
5. Нажмите **Save**

**Вариант Б - через PowerShell:**
Запустите файл `setup_database.ps1` (правой кнопкой → "Запустить с PowerShell")

## 🔑 Шаг 3: Настройте пароль в .env

**ВАЖНО:** Откройте файл `.env` и замените `ваш_пароль` на реальный пароль от PostgreSQL!

**Пример:**
Если ваш пароль `mypass123`, то строка должна быть:
```
DATABASE_URL=postgresql+asyncpg://postgres:mypass123@localhost:5432/sub_tracker
```

## 📊 Шаг 4: Создайте таблицы

После настройки .env, выполните:
```bash
python init_db.py
```

Должно появиться: **"✅ Таблицы успешно созданы!"**

## 🚀 Шаг 5: Запустите бота

```bash
python bot.py
```

Если всё правильно, вы увидите: **"Starting bot..."** и бот начнет работать! 🎉

---

## ❌ Если возникли ошибки:

### Ошибка подключения к базе данных:
- Проверьте, что база данных `sub_tracker` создана
- Проверьте, что пароль в `.env` правильный
- Убедитесь, что PostgreSQL запущен (служба `postgresql-x64-18`)

### База данных не существует:
- Создайте базу данных через pgAdmin или скрипт `setup_database.ps1`
