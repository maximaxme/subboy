# 🔐 Как изменить пароль PostgreSQL

## Способ 1: Через PowerShell скрипт (проще)

1. Запустите файл `change_postgres_password.ps1` (правой кнопкой → "Запустить с PowerShell")
2. Введите текущий пароль
3. Введите новый пароль
4. Подтвердите новый пароль

**Важно:** После изменения пароля обновите файл `.env`!

## Способ 2: Через pgAdmin (графический интерфейс)

1. Откройте **pgAdmin** (из меню Пуск)
2. Подключитесь к серверу PostgreSQL (используя текущий пароль)
3. Правой кнопкой на сервере → **Properties** (Свойства)
4. Перейдите на вкладку **Connection** (Подключение)
5. Измените пароль в поле **Password**
6. Нажмите **Save** (Сохранить)

## Способ 3: Через командную строку

1. Откройте командную строку или PowerShell
2. Найдите путь к `psql.exe` (обычно в `C:\Program Files\PostgreSQL\18\bin\`)
3. Выполните:
   ```bash
   psql -U postgres
   ```
4. Введите текущий пароль
5. Выполните SQL команду:
   ```sql
   ALTER USER postgres WITH PASSWORD 'новый_пароль';
   ```
6. Выйдите: `\q`

## Способ 4: Если забыли пароль (сброс пароля)

Если вы забыли пароль, нужно отредактировать файл `pg_hba.conf`:

1. Найдите файл `pg_hba.conf` (обычно в `C:\Program Files\PostgreSQL\18\data\`)
2. Найдите строку:
   ```
   host    all             all             127.0.0.1/32            scram-sha-256
   ```
3. Замените `scram-sha-256` на `trust`:
   ```
   host    all             all             127.0.0.1/32            trust
   ```
4. Сохраните файл
5. Перезапустите службу PostgreSQL:
   ```powershell
   Restart-Service postgresql-x64-18
   ```
6. Теперь подключитесь без пароля и установите новый:
   ```bash
   psql -U postgres
   ALTER USER postgres WITH PASSWORD 'новый_пароль';
   \q
   ```
7. **Важно:** Верните `scram-sha-256` обратно в `pg_hba.conf` и перезапустите службу!

## После изменения пароля:

**Обязательно обновите файл `.env`:**

Откройте `.env` и измените строку:
```
DATABASE_URL=postgresql+asyncpg://postgres:НОВЫЙ_ПАРОЛЬ@localhost:5432/sub_tracker
```

Замените `НОВЫЙ_ПАРОЛЬ` на пароль, который вы только что установили.

---

## Пример:

Если новый пароль `MyNewPass123`, то в `.env` должно быть:
```
DATABASE_URL=postgresql+asyncpg://postgres:MyNewPass123@localhost:5432/sub_tracker
```
