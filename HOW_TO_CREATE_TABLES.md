# 📊 Как создать таблицы - ПРОСТАЯ ИНСТРУКЦИЯ

## ⚠️ ВАЖНО: Сначала создайте базу данных!

Перед созданием таблиц нужно убедиться, что база данных `sub_tracker` существует.

### Проверка через pgAdmin:
1. Откройте **pgAdmin** (из меню Пуск)
2. Введите пароль: `1245Gufumer`
3. Посмотрите, есть ли база данных `sub_tracker` в списке
4. Если её нет - создайте:
   - Правой кнопкой на **"Databases"** → **"Create"** → **"Database"**
   - Имя: `sub_tracker`
   - Нажмите **Save**

---

## 🚀 Создание таблиц (3 способа):

### Способ 1: Двойной клик (САМЫЙ ПРОСТОЙ)

1. Откройте папку `C:\Users\evil-\sub-tracker-bot`
2. Найдите файл **`create_tables.bat`**
3. **Двойной клик** на файле
4. Дождитесь сообщения: **"✅ Таблицы успешно созданы!"**

---

### Способ 2: Через PowerShell

1. Откройте папку проекта в проводнике
2. Нажмите `Shift + Правой кнопкой мыши` в пустом месте
3. Выберите **"Открыть окно PowerShell здесь"**
4. Введите команду:
   ```bash
   python init_db.py
   ```
5. Нажмите Enter

---

### Способ 3: Через командную строку

1. Нажмите `Win + R`
2. Введите `cmd` и нажмите Enter
3. Перейдите в папку проекта:
   ```bash
   cd C:\Users\evil-\sub-tracker-bot
   ```
4. Выполните:
   ```bash
   python init_db.py
   ```

---

## ✅ Успешный результат:

После выполнения вы должны увидеть:
```
✅ Таблицы успешно созданы!
```

---

## ❌ Если ошибка:

### "connection was closed" или "ConnectionDoesNotExistError"

**Скорее всего база данных не создана!**

1. Откройте **pgAdmin**
2. Создайте базу данных `sub_tracker` (см. выше)
3. Попробуйте снова запустить `create_tables.bat`

### "password authentication failed"

Проверьте пароль в файле `.env` - он должен совпадать с паролем от PostgreSQL.

---

## 📝 После успешного создания таблиц:

Запустите бота:
```bash
python bot.py
```

Или двойной клик на файле `bot.py` (если создан .bat файл для запуска)
