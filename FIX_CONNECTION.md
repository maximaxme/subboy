# 🔧 Исправление проблемы подключения к базе данных

## Проблема:
Ошибка: `ConnectionDoesNotExistError: connection was closed in the middle of operation`

## Возможные причины и решения:

### 1. Проверьте настройки PostgreSQL (pg_hba.conf)

Файл находится обычно в: `C:\Program Files\PostgreSQL\18\data\pg_hba.conf`

Убедитесь, что есть строка:
```
host    all             all             127.0.0.1/32            scram-sha-256
```

Или для локальных подключений:
```
host    all             all             127.0.0.1/32            md5
```

### 2. Перезапустите PostgreSQL

```powershell
Restart-Service postgresql-x64-18
```

### 3. Проверьте, что порт 5432 не заблокирован

```powershell
Test-NetConnection -ComputerName localhost -Port 5432
```

### 4. Попробуйте использовать другой метод подключения

Если проблема сохраняется, можно временно использовать синхронное подключение через psycopg2.

### 5. Проверьте пароль

Убедитесь, что пароль в `.env` правильный и не содержит проблемных символов.

---

## Быстрое решение:

1. Перезапустите PostgreSQL:
   ```powershell
   Restart-Service postgresql-x64-18
   ```

2. Перезапустите бота (остановите текущий и запустите заново)

3. Попробуйте снова отправить `/start` боту

---

## Если проблема не решается:

Возможно, нужно изменить метод аутентификации в PostgreSQL или использовать другой драйвер для подключения.
