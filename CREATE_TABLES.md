# 📊 Как создать таблицы в базе данных

## Способ 1: Через командную строку/PowerShell (рекомендуется)

### Шаг 1: Откройте PowerShell или командную строку

**Вариант А - через проводник:**
1. Откройте папку `C:\Users\evil-\sub-tracker-bot`
2. Нажмите `Shift + Правой кнопкой мыши` в пустом месте
3. Выберите **"Открыть окно PowerShell здесь"** или **"Открыть окно команд здесь"**

**Вариант Б - через меню Пуск:**
1. Нажмите `Win + X`
2. Выберите **"Windows PowerShell"** или **"Терминал"**
3. Перейдите в папку проекта:
   ```powershell
   cd C:\Users\evil-\sub-tracker-bot
   ```

### Шаг 2: Выполните команду

Введите и нажмите Enter:
```bash
python init_db.py
```

### Шаг 3: Проверьте результат

Если всё правильно, вы увидите:
```
✅ Таблицы успешно созданы!
```

---

## Способ 2: Через двойной клик (если создан .bat файл)

Можно создать файл `create_tables.bat` для запуска двойным кликом.

---

## ❌ Если возникла ошибка:

### Ошибка "connection was closed" или "ConnectionDoesNotExistError":

**Проверьте:**

1. **База данных создана?**
   - Откройте pgAdmin
   - Проверьте, есть ли база данных `sub_tracker`
   - Если нет - создайте её (правой кнопкой на Databases → Create → Database)

2. **Пароль правильный?**
   - Проверьте файл `.env`
   - Убедитесь, что пароль в `DATABASE_URL` совпадает с паролем от PostgreSQL

3. **PostgreSQL запущен?**
   - Проверьте службу `postgresql-x64-18` (должна быть Running)

4. **Правильный формат DATABASE_URL?**
   - Должно быть: `postgresql+asyncpg://postgres:ПАРОЛЬ@localhost:5432/sub_tracker`
   - Без пробелов, без лишних символов

---

## 📝 Пример правильного .env файла:

```
BOT_TOKEN=8501049616:AAExoRXdQ5RW36gSmXWup0dmLcXxBX57qZ8
DATABASE_URL=postgresql+asyncpg://postgres:1245Gufumer@localhost:5432/sub_tracker
```

Обратите внимание:
- Нет пробелов
- Правильный формат URL
- Правильное имя базы данных: `sub_tracker`
