# 🚀 Инструкция по запуску бота

## Шаг 1: Установка зависимостей

Откройте PowerShell или командную строку в этой папке и выполните:

```bash
python -m pip install aiogram sqlalchemy[asyncio] asyncpg pydantic-settings python-dotenv alembic
```

**Или** запустите файл `install.bat` двойным кликом.

## Шаг 2: Настройка базы данных PostgreSQL

1. Убедитесь, что PostgreSQL установлен и запущен
2. Создайте базу данных (например, через pgAdmin или командную строку):
   ```sql
   CREATE DATABASE sub_tracker;
   ```

## Шаг 3: Настройка файла .env

Откройте файл `.env` и заполните:

1. **BOT_TOKEN** - получите токен у [@BotFather](https://t.me/BotFather) в Telegram:
   - Напишите `/newbot`
   - Следуйте инструкциям
   - Скопируйте полученный токен

2. **DATABASE_URL** - укажите данные для подключения к PostgreSQL:
   ```
   DATABASE_URL=postgresql+asyncpg://username:password@localhost:5432/sub_tracker
   ```
   Замените:
   - `username` - ваш пользователь PostgreSQL
   - `password` - ваш пароль
   - `sub_tracker` - имя вашей базы данных

## Шаг 4: Создание таблиц в базе данных

Выполните один раз:

```bash
python init_db.py
```

## Шаг 5: Запуск бота

```bash
python bot.py
```

Если всё настроено правильно, вы увидите сообщение "Starting bot..." и бот начнет работать! 🎉

---

## Возможные проблемы

### Ошибка подключения к интернету
- Проверьте интернет-соединение
- Если используете прокси, настройте pip:
  ```bash
  pip config set global.proxy http://proxy:port
  ```

### Ошибка подключения к базе данных
- Убедитесь, что PostgreSQL запущен
- Проверьте правильность данных в `.env`
- Проверьте, что база данных создана

### Модуль не найден
- Убедитесь, что все зависимости установлены (Шаг 1)
- Попробуйте переустановить: `pip install --upgrade aiogram`
