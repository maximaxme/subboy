# Telegram-бот для трекинга подписок 🚀

Бот помогает держать подписки под контролем, следить за расходами и не забывать о предстоящих списаниях.

## Основные функции
- ➕ **Добавление подписок**: Название, сумма, период (месяц/год), категория и дата списания.
- 📋 **Управление**: Просмотр списка активных подписок и их удаление.
- 🗂 **Категории**: Создание собственных категорий для группировки расходов.
- 📊 **Отчёты**: Детальная статистика расходов за месяц с учетом годовых подписок.
- 🔔 **Уведомления**: Настраиваемые напоминания за день до списания.

## Технологический стек
- **Язык**: Python 3.10+
- **Библиотека бота**: [aiogram 3.x](https://docs.aiogram.dev/)
- **База данных**: PostgreSQL
- **ORM**: SQLAlchemy 2.0 (asyncio)
- **Управление настройками**: Pydantic Settings

## Установка и запуск

1. **Клонируйте репозиторий**:
   ```bash
   git clone <repository_url>
   cd sub-tracker-bot
   ```

2. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте переменные окружения**:
   Создайте файл `.env` на основе `.env.example`:
   ```env
   BOT_TOKEN=ваш_токен_от_BotFather
   DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/dbname
   ```

4. **Запустите бота**:
   ```bash
   python bot.py
   ```

## Архитектура проекта
- `bot.py` — точка входа и инициализация.
- `handlers/` — обработчики команд и кнопок Telegram.
- `services/` — бизнес-логика и уведомления.
- `database/` — модели БД и подключение.
- `middlewares/` — прослойки для работы с БД и сессиями.
- `utils/` — состояния FSM и вспомогательные функции.
