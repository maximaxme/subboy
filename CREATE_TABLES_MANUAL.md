# 📊 Создание таблиц вручную через pgAdmin

## Проблема с подключением через Python?

Если скрипт `init_db.py` не работает из-за ошибок подключения, создайте таблицы вручную через pgAdmin.

## Инструкция:

### Шаг 1: Откройте Query Tool в pgAdmin

1. В pgAdmin найдите базу данных **`sub_tracker`** (она уже создана)
2. **Правой кнопкой** на базе данных `sub_tracker`
3. Выберите **"Query Tool"** (или нажмите `Alt+Shift+Q`)

### Шаг 2: Выполните SQL скрипт

1. Откройте файл **`create_tables.sql`** в папке проекта
2. **Скопируйте весь код** из файла
3. **Вставьте** в окно Query Tool в pgAdmin
4. Нажмите кнопку **"Execute"** (или `F5`)

### Шаг 3: Проверьте результат

После выполнения вы должны увидеть сообщение:
```
Query returned successfully in X ms.
```

### Шаг 4: Проверьте таблицы

1. В левой панели pgAdmin разверните базу данных **`sub_tracker`**
2. Разверните **"Schemas"** → **"public"** → **"Tables"**
3. Должны быть видны таблицы:
   - `users`
   - `categories`
   - `subscriptions`
   - `notification_settings`

---

## ✅ После создания таблиц:

Запустите бота:
```bash
python bot.py
```

Или двойной клик на файле `bot.py` (если создан .bat файл)
